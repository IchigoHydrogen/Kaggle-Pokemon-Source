version: pokemon-20260624-v0-06d17
baseline: v06d16 (MC winner-only, holdout=0.5014) / v06d15 (uniform CE, holdout=0.5074)
canonical_notebook: pokemon-20260624-v0-06d17.ipynb
development_track: exploration_track
runtime_mode: shadow_torch_policy
promotion_type_target: learning_promote or exploration_promote

goal: >
  Replace winner-only MC return (w=1/0) with softer outcome weighting (winner=1.0,
  loser=0.5). v06d16 showed winner-only caused underfitting (best_epoch=4, holdout
  dropped to 0.5014). Soft weighting retains loser gradient while still upweighting
  winner decisions. Hypothesis: this recovers the training depth of v06d15 while
  still biasing toward winner patterns.

hypothesis: >
  The underfitting in v06d16 was caused by 50% gradient starvation (loser w=0).
  Setting loser w=0.5 doubles the effective gradient signal while preserving the
  winner/loser asymmetry. Expected result: best_epoch recovers toward v06d15 range
  (15-25), holdout top-1 >= v06d15 (0.5074), winner/loser gap persists.

change_scope: >
  Exactly one change from v06d16: in the batch loop, change
  _w_tensor = torch.tensor(_b_won, ...)   [winner=1, loser=0]
  to
  _w_tensor = torch.tensor(0.5 + 0.5 * _b_won, ...)  [winner=1, loser=0.5]
  Everything else (features, architecture, deck, split, diagnostics) unchanged.

fresh_training: true

artifact_reuse_policy: >
  Reuse raw episode files, card metadata, rule agent source, deck, v06d16 notebook
  as template. No prior weights, predictions, or thresholds reused.

datasets: Same episode files and deterministic split as v06d15/v06d16.

success_criteria:
  - Notebook executes end-to-end.
  - Feature cross-check passes.
  - best_epoch > 4 (no premature underfitting).
  - holdout_model_top1 >= 0.5074 (at or above v06d15 uniform CE).
  - winner/loser holdout gap remains >= 0.010.
  - Shadow probe: zero illegal actions, zero exceptions, action_changes=0.

rollback_criteria:
  - Notebook failure.
  - Feature cross-check mismatch.
  - best_epoch <= 4 again (same underfitting).
  - holdout_model_top1 < 0.47.

expected_artifacts:
  - executed notebook
  - main_model_report.json
  - main_mc_return_report.json (winner_frac, winner/loser top-1)
  - baseline-comparison.json
  - promotion-decision.json
  - shadow probe outputs (action_changes=0)
  - prefixed submission archives
