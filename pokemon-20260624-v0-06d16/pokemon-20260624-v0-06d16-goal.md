version: pokemon-20260624-v0-06d16
baseline: pokemon-20260624-v0-06d11 (runtime model baseline) / pokemon-20260624-v0-06d15 (methodology baseline)
canonical_notebook: pokemon-20260624-v0-06d16.ipynb
development_track: exploration_track
runtime_mode: shadow_torch_policy
promotion_type_target: learning_promote or exploration_promote

goal: >
  Replace uniform per-decision CE loss with outcome-weighted CE (Monte Carlo return:
  w=1.0 for winner decisions, w=0.0 for loser decisions). Measure whether training
  only on winner-side decisions improves replay holdout top-1 accuracy without
  changing features, architecture, or runtime behavior.

hypothesis: >
  Filtering training to winner-side decisions (MC return = game outcome) removes
  noise from loser actions and shifts the model toward 'what winning players do'.
  This should improve holdout top-1 over uniform CE (v06d15: 0.5074) without
  requiring any feature or architecture change.

change_scope: >
  Exactly one change from v06d15: training loss weighting. Extract _tr_won from
  train split. In the training batch loop, replace F.cross_entropy(_logits,
  _chosen_t) with outcome-weighted CE (reduction='none', multiply by won-indicator,
  normalize by winner count). Feature vector (96-dim), architecture, deck,
  deterministic split, and all diagnostics remain identical to v06d15.

fresh_training: true

artifact_reuse_policy: >
  Reuse raw replay/log episode files, card metadata, baseline rule agent source,
  deck definition, and v06d15 notebook as template. Do not reuse prior model
  weights, predictions, thresholds, or promotion metrics. Train entirely fresh
  under v06d16 RUN_PREFIX.

datasets: >
  Same episode JSON files and deterministic episode-hash train/valid/holdout split
  as v06d15. Top200 ranking CSV unchanged.

success_criteria:
  - Canonical v06d16 notebook executes end-to-end.
  - Fresh v06d16 model trained under current RUN_PREFIX.
  - Feature cross-check passes (zero mismatches).
  - Winner fraction in train split reported (expect 0.45-0.55).
  - Effective winner decision count in train >= 5000.
  - Holdout top-1 compared to v06d15 (0.5074) and v06d11 (0.509).
  - Holdout winner-side and loser-side top-1 reported separately.
  - Shadow runtime probe: zero illegal actions, zero exceptions, action_changes=0.
  - Promotion decision records whether MC return weighting helped/neutral/hurt.

rollback_criteria:
  - Notebook execution failure.
  - Feature cross-check mismatch.
  - Effective winner decision count in train < 5000.
  - Holdout top-1 < 0.47.
  - Training loss diverges or collapses (loss=0 from first batch).
  - Any feature, architecture, or deck change detected.

expected_artifacts:
  - executed canonical notebook
  - main_model_report.json
  - main_feature_spec.json (loss field: mc_return_winner_only)
  - main_dataset_report.json
  - main_holdout_predictions.parquet
  - main_mc_return_report.json
  - baseline-comparison.json
  - promotion-decision.json
  - shadow runtime probe outputs (action_changes=0)
  - prefixed submission archives
