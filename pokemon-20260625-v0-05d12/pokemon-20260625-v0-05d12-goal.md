{
  "version": "pokemon-20260625-v0-05d12",
  "hypothesis": "Early-game filter T/2 (step_fraction <= 0.5) - more training data than v05d11 T/3 (5 entries, 9% coverage) while still focusing on impactful early decisions. Expected to give 10-20 table entries with coverage ~30-40%. Also fixes f-string set-literal bug so smoke test succeeds and live_hit_rate is now measurable.",
  "bug_fix": "KEYWORDS = set([...]) instead of {\"END\",...} which becomes tuple inside helper f-string",
  "expected": "winner_margin between 0.0852 (v05d10 full) and 0.0958 (v05d11 T/3), coverage ~30-40%",
  "causal_assumption": "T/2 = more data than T/3, less noise than full data. Coverage-margin tradeoff is better.",
  "changes": [
    "step_fraction <= 0.5 (vs 0.333 in v05d11)",
    "KEYWORDS=set([]) bug fix in submission helper"
  ]
}