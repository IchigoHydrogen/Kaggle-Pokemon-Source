version: pokemon-20260624-v0-06d18
baseline: pokemon-20260624-v0-06d15 (uniform CE, holdout=0.5074)
canonical_notebook: pokemon-20260624-v0-06d18.ipynb
development_track: exploration_track
runtime_mode: shadow_torch_policy
promotion_type_target: learning_promote

goal: >
  Return-conditioned BC with Top200 vs Top200 filter.
  (1) Add won (0/1) as feature dim 96 at train; always set won=1.0 at inference.
  Model learns P(action|state,win=1) without conflicting gradients.
  (2) Filter episodes to Top200 vs Top200 only, reducing weak-player noise.
  Uniform CE loss unchanged from v06d15.

hypothesis: >
  won as a feature (not a loss weight) teaches winning-player behavior without
  the gradient conflict that killed v06d16/v06d17. Top200 filter raises signal
  quality. Combined: holdout top-1 > v06d15 (0.5074).

change_scope: >
  feat_dim 96->97, won at dim 96 (train=actual, infer=1.0), episode Top200
  filter (both players rank<=200), runtime Linear(96,512)->Linear(97,512).
  Uniform CE, architecture depth, features [0-95], deck, split unchanged.

fresh_training: true
datasets: Same episode files. Top200 CSV for rank lookup and episode filter.

success_criteria:
  - Feature cross-check passes (zero mismatches in 97-dim vectors).
  - Train decisions >= 5000 after filter.
  - holdout_model_top1 > v06d15 (0.5074) OR best_epoch > 10.
  - Shadow probe: zero illegal, exceptions, action_changes.

rollback_criteria:
  - Notebook failure or cross-check mismatch.
  - Train decisions < 5000.
  - holdout_model_top1 < 0.47 or best_epoch <= 4.
