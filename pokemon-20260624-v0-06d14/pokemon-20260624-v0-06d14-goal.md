version: pokemon-20260624-v0-06d14
baseline: pokemon-20260624-v0-06d13
canonical_notebook: pokemon-20260624-v0-06d14.ipynb
runtime_mode: guarded_torch_policy
promotion_type_target: runtime_promote_candidate

goal: >
  Test a narrow END deferral runtime guard: when the rule agent selects END in MAIN,
  allow a trained v06d12/v06d13-style scorer to override only to safe non-END action
  families with confidence gating. Do not change ATTACK-selected decisions.

hypothesis: >
  v06d13 showed a strong holdout signal that rule-selected END is often premature.
  If END deferral is restricted to safe families (PLAY, ATTACH, EVOLVE, RETREAT) and
  selected using valid-only thresholds, the runtime can improve replay/log holdout
  behavior without the ATTACK cross-type contamination that broke v06d12.

change_scope: >
  Keep CE loss, card metadata features, model architecture, deck, raw datasets, and
  deterministic split unchanged. Add a safe-END-deferral threshold grid and runtime
  guard. Runtime override is allowed only when rule_option_type is END and model_top1
  option type is PLAY, ATTACH, EVOLVE, or RETREAT. ABILITY, ATTACK, END, DISCARD, and
  all other families remain blocked.

fresh_training: true

artifact_reuse_policy: >
  Reuse raw replay/log episode files, card metadata via all_card_data(), baseline rule
  agent source code, deck definition, and v06d13 source/notebook as a template. Do not
  reuse prior predictions, thresholds, model weights, generated main.py, or promotion
  metrics as current evidence. A fresh model is trained under the v06d14 RUN_PREFIX.

datasets: >
  Same episode JSON files as v06d13. Top200 ranking CSV. Same deterministic episode-hash
  train/valid/holdout split.

success_criteria:
  - Canonical v06d14 notebook executes end-to-end.
  - Fresh v06d14 model artifacts are produced under the current RUN_PREFIX.
  - Feature crosscheck passes with zero mismatches.
  - Holdout model_top1 remains >= 0.49.
  - Valid-only threshold selection for safe END deferral is recorded.
  - Holdout safe END deferral has positive benefit_minus_harm and non-negative delta.
  - Runtime action changes are nonzero and only occur from rule END to safe non-END types.
  - Smoke eval has zero illegal actions and zero exceptions.
  - Archive loader validation passes and archived main.py compiles.
  - Promotion decision records whether this is runtime_promote, needs_followup, or reject.

rollback_criteria:
  - Any illegal action or exception in smoke.
  - Runtime changes any rule ATTACK-selected decision.
  - Runtime changes any non-END rule-selected decision.
  - Runtime changes END to ABILITY, ATTACK, END, DISCARD, or unknown unsafe families.
  - Holdout safe END deferral benefit_minus_harm <= 0.
  - Holdout safe END deferral delta < 0.
  - Notebook execution, archive, loader, or feature crosscheck fails.

expected_artifacts:
  - executed canonical notebook
  - main_feature_spec.json
  - main_dataset_report.json
  - main_model_report.json
  - main_hybrid_report.json
  - end_deferral_report.json
  - end_deferral_threshold_grid.parquet
  - end_deferral_safe_report.json
  - end_deferral_safe_threshold_grid.parquet
  - main_runtime_probe_report.json
  - kaggle_loader_validation_report.json
  - promotion-decision.json
  - prefixed submission archive
