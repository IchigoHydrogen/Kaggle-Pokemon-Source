# v0-06d11 Version Goal

version: pokemon-20260624-v0-06d11
baseline: pokemon-20260624-v0-06d10
canonical_notebook: pokemon-20260624-v0-06d11.ipynb
runtime_mode: guarded_torch_policy
promotion_type_target: runtime_promote

goal: Fix the Kaggle validation failure by making the generated submission archive compatible with kaggle_environments raw Python loading, while keeping the v0-06d10 policy, model architecture, feature definition, deck, thresholds, and guarded override scope unchanged.

hypothesis: The v0-06d10 validation episode failed because kaggle_environments executes the last callable defined in raw main.py, and the generated main.py's last callable was `_v06d10_features_batch` rather than the intended `agent`. Adding an explicit final entrypoint callable in the generated main.py and verifying the archive through `get_last_callable` should prevent the step-1 crash without changing policy strength.

change_scope:
- Bump RUN_PREFIX and canonical notebook to v0-06d11.
- Re-run fresh MAIN scorer training under the v0-06d11 prefix.
- Generate guarded_torch_policy main.py from the notebook, not by manually repacking an existing archive.
- Append a final Kaggle loader entrypoint callable after every helper function in generated main.py.
- Add archive-level validation that `kaggle_environments.agent.get_last_callable` selects the intended final entrypoint.
- Add a fixture check using the downloaded 81567646 step-0 observation/configuration shape.
- Keep v0-06d10 model architecture, features, thresholds, deck, safe override types, hard veto types, and local smoke protocol unchanged.

fresh_training: true

artifact_reuse_policy: Reuse raw replay/log datasets, card metadata, sample submission cg files, base rule source generated inside the current notebook run, and deck only. Do not reuse v0-06d10 weights, predictions, thresholds selected from v0-06d10 artifacts, or promotion metrics as current-run evidence. v0-06d10 metrics may be used only as baseline comparison.

datasets:
- Pokemon TCG AI Battle card data and sample submission under /kaggle/input.
- top200 ranking data.
- replay episode JSONs under /kaggle/input/competitions/pokemon-tcg-ai-battle-episodes-2026-06-22.
- downloaded failed validation episode fixture files 81567646*.json for entrypoint crash reproduction only, not for training or threshold tuning.

success_criteria:
- `pokemon-20260624-v0-06d11.ipynb` executes end-to-end.
- Fresh model is trained under /kaggle/working/pokemon-20260624-v0-06d11/.
- The final archive is `/kaggle/working/pokemon-20260624-v0-06d11-submission.tar.gz`.
- Archive contains main.py, deck.csv, cg/api.py, cg/game.py, and models/main_option_scorer.pt.
- Archive has no __pycache__, .pyc, .pyo, or Zone.Identifier files.
- `get_last_callable` on archived main.py returns the intended final entrypoint, not a helper.
- The final entrypoint accepts a one-argument observation call and a two-argument observation/configuration call.
- The 81567646 step-0 fixture call returns a list action and does not raise.
- Local smoke evaluation has zero illegal actions and zero exceptions.
- Runtime feature crosscheck passes.
- Holdout quality is not materially worse than v0-06d10.
- Promotion decision JSON records loader validation results.

rollback_criteria:
- The notebook does not execute end-to-end.
- Archive loader validation still selects a helper callable.
- The fixture reproduces an exception.
- Local smoke evaluation has illegal actions or exceptions.
- main.py fails to compile.
- Model loading fails without a clean rule-only fallback.
- Holdout quality materially regresses relative to v0-06d10.
- The implementation changes policy behavior beyond the entrypoint fix.

expected_artifacts:
- executed notebook
- split manifest/report artifacts
- main dataset/model/hybrid reports
- runtime probe report with Kaggle loader validation
- fixture validation report
- generated main.py in the archive
- generated deck.csv in the archive
- prefixed guarded submission archive
- promotion decision JSON
- loop record
